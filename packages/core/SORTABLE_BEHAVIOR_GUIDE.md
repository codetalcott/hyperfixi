# Sortable Behavior Guide — moved

> **This document is obsolete.** It described an earlier imperative implementation of
> the Sortable behavior. Sortable is now defined in pure hyperscript `source`
> ([sortable.schema.ts](../behaviors/src/schemas/sortable.schema.ts)) and compiles
> from that single source like every other behavior.

For authoring and installing behaviors, see the canonical guide:
**[packages/behaviors/AUTHORING.md](../behaviors/AUTHORING.md)**.
